// Ejemplo de `config.js` para uso local
// Copia este archivo a `config.js` y reemplaza la apiKey por la tuya.
// No subas `config.js` al repositorio.
window.SR = window.SR || {};
window.SR.config = window.SR.config || {
  apiKey: 'TU_API_KEY_AQUI'
};
